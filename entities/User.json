{
  "name": "User",
  "type": "object",
  "properties": {
    "id": {
      "type": "string",
      "description": "ID único do usuário"
    },
    "nome": {
      "type": "string",
      "description": "Campo canônico: nome completo do usuário"
    },
    "full_name": {
      "type": "string",
      "description": "Alias legado aceito temporariamente na borda. Deve ser normalizado internamente para `nome`."
    },
    "email": {
      "type": "string",
      "format": "email",
      "description": "Email do usuário"
    },
    "senha": {
      "type": "string",
      "description": "Senha criptografada do usuário"
    },
    "perfil": {
      "type": "string",
      "enum": ["admin", "colaborador", "produtor"],
      "default": "produtor",
      "description": "Perfil de acesso: admin (Bruna/César - acesso total Brasil), colaborador (acesso regional, mesmas funções do admin), produtor (= cliente = proprietário - dono da fazenda, apenas visualização/download + caderno de campo)"
    },
    "regiao": {
      "type": "string",
      "description": "Região principal do colaborador - ex: Goiás, Mato Grosso, Sul"
    },
    "sub_regioes": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Sub-regiões do colaborador - ex: Goiás 1, Goiás 2, Goiânia, Rio Verde"
    },
    "regioes_acesso": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Lista de regiões que o admin tem acesso (normalmente ['Brasil'])"
    },
    "produtor_id": {
      "type": "string",
      "description": "Campo canônico do vínculo ao produtor titular (para perfil produtor). Um produtor pode ter várias fazendas. Várias pessoas (pai, mãe) podem ter login vinculado ao mesmo produtor."
    },
    "telefone": {
      "type": "string",
      "description": "Telefone de contato do usuário"
    },
    "ativo": {
      "type": "boolean",
      "default": true,
      "description": "Se o usuário está ativo no sistema"
    },
    "data_cadastro": {
      "type": "string",
      "format": "date-time",
      "description": "Data de cadastro do usuário"
    }
  },
  "required": ["email", "senha", "perfil"],
  "anyOf": [
    { "required": ["nome"] },
    { "required": ["full_name"] }
  ]
}
